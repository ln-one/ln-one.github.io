{
  "name": "chunran-zhang-academic-website",
  "private": true,
  "scripts": {
    "dev": "npm run cv && sh scripts/jekyll.sh serve --host 127.0.0.1 --port 4321",
    "build": "npm run cv && sh scripts/jekyll.sh build --strict_front_matter",
    "check": "python3 scripts/check_site.py",
    "cv": "sh scripts/build-cv.sh"
  }
}
